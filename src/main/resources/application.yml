springdoc:
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    url: /docs/open-api-3.0.1.json
    path: /docs/swagger

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/keyword
    username: keyword
    password: soni
  jpa:
    properties:
      hibernate:
        format_sql: 'true'
        ddl-auto: create
    show-sql: 'true'
  flyway:
    enabled: true
    user: keyword
    password: soni
    baselineOnMigrate: true